<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.jkmaks.myuw">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.jkmaks.activities.StartingActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.jkmaks.activities.AddClassActivity"
            android:label="@string/title_activity_add_class" >
        </activity>
        <activity
            android:name="com.jkmaks.activities.MainActivity"
            android:label="MyUW" >
        </activity>
        <activity
            android:name="com.jkmaks.activities.Events"
            android:label="@string/title_activity_events" >
        </activity>
    </application>


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>
